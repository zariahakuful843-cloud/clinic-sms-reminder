# AGENTS.md

## Cursor Cloud specific instructions

### Repository Status

This repository (`clinic-sms-reminder`) is currently an empty scaffold with only a `README.md`. There is no application code, no dependency files, no build system, and no tests.

### Available System Tooling

- **Node.js**: v22.x (via system install)
- **Python**: 3.12.x
- **Git**: 2.43.x
- **npm**: 10.x

### Development Notes

- No `package.json`, `requirements.txt`, or other dependency manifest exists yet.
- No lint, test, or build commands are available until application code is added.
- When application code is introduced, update this file with startup/run caveats and the update script via `SetupVmEnvironment`.
